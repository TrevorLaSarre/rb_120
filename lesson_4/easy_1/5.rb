class Fruit
  def initialize(name)
    name = name
  end
end

class Pizza
  def initialize(name)
    @name = name
  end
end

# The Pizza class above has an instance variable (@name). You can tell because it is proceeded by the @ symbol. 